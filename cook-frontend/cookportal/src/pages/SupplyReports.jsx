import React from "react";

export default function SupplyReports() {
  return (
    <div className="page">
      <div className="card">
        <h2 style={{marginTop:0}}>Supply Reports (Upcoming)</h2>
        <p style={{color:"var(--muted)"}}>Future development will link meal orders to inventory to generate re-order lists.</p>
      </div>
    </div>
  );
}
