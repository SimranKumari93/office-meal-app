import React from "react";

export default function DailyOrders() {
  return (
    <div className="page card">
      <h2 style={{marginTop:0}}>Daily Orders Board</h2>
      <p style={{color:"var(--muted)"}}>Today's orders summary (UI stub).</p>
      {/* add tables/charts here later */}
    </div>
  );
}
